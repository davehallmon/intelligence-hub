# Lens Styles

This directory contains styles owned by visible v10 lenses. Each stylesheet is loaded by its corresponding module under `js/lenses/`, preserving the existing progressive-enhancement and cascade behavior.

Shared tokens and interaction-shell styles remain under `css/`. Production-active compatibility styles remain at the repository root until replacement parity and regression evidence support their migration or removal.
